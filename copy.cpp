void copy(const auto& srcBegin, const auto& srcEnd, auto& dstBegin) {
    /*TODO
        Get size of source
        Get size of destination
        Handle deep copying? Do users prefer shallow copy? Possibly check if destination underlying type is the non-pointer type of source and then do deep copy
        */
}
